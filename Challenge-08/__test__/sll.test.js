'use strict';

const SLL = require('../lib/sll');

describe('SLL Module', () => {
  describe('SLL', () => {
    it ('should accept two objects as the arguments', () => {
      expect().toBe();
    });
  });
});