'use strict';


const solution = module.exports = {};
const SLL = require('../lib/SLL');

solution.sortedBiTree = function (binaryTree) {
  if (!binaryTree) return null;
  let sll = new SLL();
  console.log(sll);
};